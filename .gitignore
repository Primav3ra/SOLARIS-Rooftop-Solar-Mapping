# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/
ENV/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.hypothesis/

# Node / frontend
node_modules/
.vite/
*.tsbuildinfo

# Built frontend bundles are generated, not source (see docs/architecture.md)
app/static/intro-build/
src/solaris/api/static/intro-build/

# Secrets and local config
.env
.env.*
!.env.example
*-service-account.json
*credentials*.json
gee-key*.json

# ML artifacts (versioned in GCS; see models/MANIFEST.json)
models/**/*.joblib
models/**/*.pkl
models/**/*.txt.model
models/**/data/

# Eval outputs are CI artifacts, except the latest report -- that is the
# project's evidence artifact and the README links to it, so it is committed.
/reports/
evals/reports/*
!evals/reports/latest.md
!evals/reports/latest.json

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp

# Scratch
scratch/
tmp/
*.log
